/** Must stay in sync with the `purpose` enum in `/api/uploads/sign`. */
export type UploadPurpose =
  | "branding"
  | "favicon"
  | "vehicle-images"
  | "vehicle-videos"
  | "vehicle-documents"
  | "brands"
  | "body-types"
  | "collections"
  | "testimonials";
