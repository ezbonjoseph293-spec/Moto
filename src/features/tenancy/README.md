# tenancy

Multi-tenant foundation: dealership resolution from `/{dealerSlug}`, the
tenant-scoped repository layer (all dealer-owned data access goes through it),
and isolation guarantees. Built in Stages 1 and 3.
