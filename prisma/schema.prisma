// Moto Platform — Prisma schema
// Data models are designed and added in Stage 1 (schema & tenancy foundation).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
